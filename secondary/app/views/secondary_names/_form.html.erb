<%= form_with(model: secondary_name) do |form| %>
  <% if secondary_name.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(secondary_name.errors.count, "error") %> prohibited this secondary_name from being saved:</h2>

      <ul>
        <% secondary_name.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
